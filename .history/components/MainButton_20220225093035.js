import React from 'react'
import { View, Text, StyleSheet, Button, TouchableOpacity } from 'react-native'

function MainButton(props) {
    return <TouchableOpacity>MainButton</TouchableOpacity>
}

export default MainButton
